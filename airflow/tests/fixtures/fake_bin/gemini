#!/usr/bin/env python3
import sys

args = sys.argv[1:]
prompt = args[0] if args else ""
if prompt == "TRIGGER_FAILURE":
    print("simulated failure", file=sys.stderr)
    sys.exit(1)
print(f"gemini response for: {prompt}")
